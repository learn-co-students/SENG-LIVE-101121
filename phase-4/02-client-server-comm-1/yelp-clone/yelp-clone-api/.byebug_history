c
@business
c
request.url
ApplicationController? >>   def find
    if params.path.include?('reviews')
      Review.find(params[:id])
    elsif params.path.include?('users')
      User.find(params[:id])
    elsif params.path.include?('businesses')
      Business.find(params[:id])
    end
  end
request.path.include?("businesses")
request.path
params.full_path
params.path
params
